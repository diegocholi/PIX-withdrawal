# Bin

Convencao inicial para arquivos executaveis:

- `hyperf` para o binario principal da aplicacao
- `hyperf.php` para o bootstrap PHP consumido pelo Hyperf
- `bootstrap-http`
- `bootstrap-cli`
- `bootstrap-<runtime>`

Usar `kebab-case` e nomes orientados ao runtime executado.
