# Data Migrations

Migrations do schema relacional do modulo.

Responsabilidades:

- criar e evoluir tabelas do dominio
- definir indices, chaves e constraints
- manter o schema rastreavel e reproduzivel em ambiente Docker
- expor a ordem canonica do schema em `DataSchemaMigrationPlan`
