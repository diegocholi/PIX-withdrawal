# HTTP Public DTO

Payloads publicos do contrato HTTP.

Regras:

- requests usam sufixo `RequestPayload`
- responses usam sufixo `ResponsePayload`
- chaves serializadas devem permanecer em `snake_case`
