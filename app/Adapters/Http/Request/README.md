# HTTP Requests

Objetos e componentes de validacao de entrada da camada HTTP.

Regras:

- validar formato, obrigatoriedade e coerencia basica do payload
- normalizar dados de transporte antes do mapeamento para o core
- nao duplicar invariantes que pertencem ao dominio
